[package]
name = "xtask"
version.workspace = true
edition.workspace = true

[dependencies]
clap             = { version = "4", features = ["derive"] }
common = { path = "../common" }
sha2             = "0.10"
hex              = "0.4"

[dev-dependencies]
tempfile = "3"
