[package]
name = "xtask"
version.workspace = true
edition.workspace = true

[dependencies]
clap             = { version = "4", features = ["derive"] }
common = { path = "../common" }

[dev-dependencies]
tempfile = "3"
