[package]
name = "xtask"
version.workspace = true
edition.workspace = true

[dependencies]
# clap for subcommand parsing; no flashpoint-common dep needed here
clap = { version = "4", features = ["derive"] }
